server:
  port: 9090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tnt_shop?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root  # 修改为你的数据库账号
    password: 123456  # 修改为你的数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true